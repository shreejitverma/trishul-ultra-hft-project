\begin{tikzpicture}[node distance=1.5cm, auto, >=latex']
    \tikzstyle{reg} = [rectangle, draw, fill=yellow!10, text width=4em, text centered, minimum height=2em]
    \tikzstyle{logic} = [rectangle, draw, fill=red!10, text width=6em, text centered, rounded corners]
    \tikzstyle{mem} = [rectangle, draw, fill=blue!10, text width=5em, text centered, minimum height=4em]

    % Inputs
    \node [reg] (bbo) {BBO Regs};
    \node [reg, below=of bbo] (pos) {Inventory};
    \node [reg, below=of pos] (vol) {Volatility};

    % Feature Extraction
    \node [logic, right=of pos] (feat) {Feature Vector};
    
    % Inference Core
    \node [mem, right=of feat, yshift=1cm] (weights) {Weight ROM};
    \node [logic, right=of feat] (dsp) {DSP48 (MAC)};
    \node [logic, right=of dsp] (act) {Activation (ReLU/LUT)};

    % Decision
    \node [logic, right=of act] (comp) {Comparator};
    \node [reg, right=of comp] (out) {Signal};

    % Wiring
    \path [draw, ->] (bbo) -| (feat);
    \path [draw, ->] (pos) -- (feat);
    \path [draw, ->] (vol) -| (feat);
    
    \path [draw, ->] (feat) -- (dsp);
    \path [draw, ->] (weights) -- (dsp);
    \path [draw, ->] (dsp) -- (act);
    \path [draw, ->] (act) -- (comp);
    \path [draw, ->] (comp) -- (out);

\end{tikzpicture}
