\begin{tikzpicture}[node distance=1.5cm, auto]
    \tikzstyle{block} = [rectangle, draw, fill=cyan!10, text width=7em, text centered, rounded corners, minimum height=3em]
    \tikzstyle{logic} = [rectangle, draw, fill=red!10, text width=6em, text centered, rounded corners, minimum height=3em]
    \tikzstyle{line} = [draw, -latex', thick]

    \node [block] (input) {Tick Queue};
    \node [block, below=of input] (ts) {Timestamping Unit};
    
    % Logic Blocks
    \node [logic, right=of ts] (mm) {Market-Making Logic (RL)};
    \node [logic, above=of mm] (arb) {Arbitrage Logic};
    \node [logic, below=of mm] (stuff) {Quote-Stuffing Logic};
    
    \node [block, right=of mm] (decision) {Decision Engine};
    \node [block, right=of decision] (output) {Order Output};

    \path [line] (input) -- (ts);
    \path [line] (ts) -- (mm);
    \path [line] (ts) -- (arb);
    \path [line] (ts) -- (stuff);
    
    \path [line] (mm) -- (decision);
    \path [line] (arb) -- (decision);
    \path [line] (stuff) -- (decision);
    
    \path [line] (decision) -- (output);

\end{tikzpicture}
