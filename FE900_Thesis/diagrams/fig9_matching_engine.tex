\begin{center}
\resizebox{0.98\textheight}{!}{
\begin{tikzpicture}[node distance=2.5cm, auto, >=latex', thick]
\tikzset{
    hw_block/.style={rectangle, draw, fill=red!10, text width=8em, text centered, rounded corners, minimum height=4em},
    logic_block/.style={rectangle, draw, fill=blue!10, text width=8em, text centered, rounded corners, minimum height=4em},
    line/.style={draw, -latex', line width=1.5pt}
}

\node [hw_block] (rx) {MAC RX \\ (AXI-Stream)};
\node [logic_block, right=of rx] (parser) {Protocol \\ Parser};
\node [logic_block, right=of parser] (decoder) {ITCH 5.0 \\ Decoder};
\node [logic_block, right=of decoder] (book) {L2 Order \\ Book};

\node [logic_block, right=of book, xshift=1cm] (strat) {\textbf{RL Inference} \\ \textbf{Engine}};

\node [logic_block, right=of strat, xshift=1cm] (encoder) {OUCH 2.0 \\ Encoder};
\node [logic_block, right=of encoder] (bridge) {AXI-Bridge \\ (TX)};
\node [hw_block, right=of bridge] (tx) {MAC TX \\ (AXI-Stream)};

\path [line] (rx) -- (parser);
\path [line] (parser) -- node[above, midway] {Payload} (decoder);
\path [line] (decoder) -- node[above, midway] {Tick} (book);
\path [line] (book) -- node[above, midway] {State} (strat);
\path [line] (strat) -- node[above, midway] {Order} (encoder);
\path [line] (encoder) -- (bridge);
\path [line] (bridge) -- (tx);

\node [draw, dashed, fit=(rx) (tx), label=above:FPGA Matching Engine Pipeline] {};

\end{tikzpicture}
}
\end{center}