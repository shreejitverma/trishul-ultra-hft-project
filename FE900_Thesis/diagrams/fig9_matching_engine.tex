\begin{center}
\resizebox{0.95\textheight}{!}{
\begin{tikzpicture}[node distance=2.5cm, auto, >=latex', thick]
\tikzset{
    hw_block/.style={rectangle, draw, fill=red!10, text width=6em, text centered, rounded corners, minimum height=4em},
    logic_block/.style={rectangle, draw, fill=blue!10, text width=6em, text centered, rounded corners, minimum height=4em},
    line/.style={draw, -latex', line width=1.5pt}
}

\node [hw_block] (rx) {MAC RX \\ (AXI-Stream)};
\node [logic_block, right=of rx, xshift=-0.5cm] (parser) {Protocol \\ Parser};
\node [logic_block, right=of parser, xshift=-0.5cm] (decoder) {ITCH 5.0 \\ Decoder};
\node [logic_block, right=of decoder, xshift=-0.5cm] (book) {L2 Order \\ Book};

\node [logic_block, right=of book, xshift=-0.2cm] (strat) {\textbf{RL Inference} \\ \textbf{Engine}};

\node [logic_block, right=of strat, xshift=-0.2cm] (encoder) {OUCH 2.0 \\ Encoder};
\node [logic_block, right=of encoder, xshift=-0.5cm] (bridge) {AXI-Bridge \\ (TX)};
\node [hw_block, right=of bridge, xshift=-0.5cm] (tx) {MAC TX \\ (AXI-Stream)};

\path [line] (rx) -- (parser);
\path [line] (parser) -- node[above, midway, scale=0.7] {Payload} (decoder);
\path [line] (decoder) -- node[above, midway, scale=0.7] {Tick} (book);
\path [line] (book) -- node[above, midway, scale=0.7] {State} (strat);
\path [line] (strat) -- node[above, midway, scale=0.7] {Order} (encoder);
\path [line] (encoder) -- (bridge);
\path [line] (bridge) -- (tx);

\node [draw, dashed, fit=(rx) (tx), inner sep=0.3cm, label=above:FPGA Matching Engine Pipeline] {};

\end{tikzpicture}
}
\end{center}
