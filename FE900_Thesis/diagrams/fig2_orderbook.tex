\begin{center}
\resizebox{0.95\textheight}{!}{
\begin{tikzpicture}[node distance=2.5cm, auto, >=latex', thick]
\tikzset{
    process/.style={rectangle, draw, fill=green!10, text width=8em, text centered, rounded corners, minimum height=4em},
    memory/.style={rectangle, draw, fill=yellow!10, text width=8em, text centered, minimum height=4em},
    stream/.style={draw, -latex', line width=1.5pt}
}

\node [process] (feed) {Normalized \\ Tick Stream};
\node [process, right=of feed] (logic) {Book Update \\ Logic};
\node [memory, above=of logic, yshift=0.5cm] (map) {Order ID Map \\ (HashMap)};
\node [memory, below=of logic, yshift=-0.5cm] (levels) {Price Levels \\ (Flat Array)};

\node [process, right=of logic] (diff) {Diff Generator};
\node [process, right=of diff] (pub) {Event \\ Publisher};

\node [process, above right=of pub, yshift=-0.5cm] (strat) {Strategy \\ Engine};
\node [process, below right=of pub, yshift=0.5cm] (risk) {Risk \\ Monitor};

\path [stream] (feed) -- (logic);
\path [stream, <->] (logic) -- (map);
\path [stream, <->] (logic) -- (levels);
\path [stream] (logic) -- node[above, midway] {Delta} (diff);
\path [stream] (diff) -- (pub);
\path [stream] (pub) -- (strat);
\path [stream] (pub) -- (risk);

\end{tikzpicture}
}
\end{center}
