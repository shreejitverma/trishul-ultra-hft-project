\begin{tikzpicture}[node distance=1.5cm, auto]
    % Styles
    \tikzstyle{block} = [rectangle, draw, fill=blue!10, text width=8em, text centered, rounded corners, minimum height=3em]
    \tikzstyle{cloud} = [draw, ellipse, fill=red!10, node distance=3cm, minimum height=2em]
    \tikzstyle{line} = [draw, -latex', thick]

    % Nodes
    \node [cloud] (exchange) {Exchange (NASDAQ/NYSE)};
    \node [block, below=of exchange] (nic) {Ultra-Low Latency NIC (FPGA/SmartNIC)};
    \node [block, below=of nic] (bypass) {Kernel Bypass (DPDK/Solarflare)};
    \node [block, below=of bypass] (handler) {Market Data Feed Handler};
    \node [block, below=of handler] (norm) {Normalization & Decoding};

    % Connections
    \path [line] (exchange) -- node {Multicast Feed} (nic);
    \path [line] (nic) -- node {DMA (No OS)} (bypass);
    \path [line] (bypass) -- node {Raw Packets} (handler);
    \path [line] (handler) -- (norm);
    
    % Grouping
    \begin{scope}[on background layer]
        \node [fit=(nic) (norm), fill=yellow!10, rounded corners, label=above:Co-Location Server] {};
    \end{scope}
\end{tikzpicture}
