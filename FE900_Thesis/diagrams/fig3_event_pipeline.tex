\begin{tikzpicture}[node distance=1.5cm, auto]
    \tikzstyle{block} = [rectangle, draw, fill=blue!10, text width=8em, text centered, rounded corners, minimum height=3em]
    \tikzstyle{line} = [draw, -latex', thick]

    \node [block] (book) {In-Memory Order Book};
    \node [block, below=of book] (queue) {Lock-Free Queue};
    \node [block, below=of queue] (stamp) {Nanosecond Timestamping};
    \node [block, below=of stamp] (multicast) {Event Multicast};
    
    % Consumers
    \node [block, right=of multicast, yshift=1.5cm] (downstream) {Downstream Systems};
    \node [block, right=of multicast] (external) {External Systems};
    \node [block, right=of multicast, yshift=-1.5cm] (monitor) {Latency Monitor};

    \path [line] (book) -- (queue);
    \path [line] (queue) -- (stamp);
    \path [line] (stamp) -- (multicast);
    
    \path [line] (multicast) -- (downstream);
    \path [line] (multicast) -- (external);
    \path [line] (multicast) -- (monitor);

\end{tikzpicture}

